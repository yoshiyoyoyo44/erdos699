# i=3：全ての奇数 M に対する平方の枝の排除

2026-09-12。前稿の M=1,3 という制限を外す。
**これは平方の枝の完全な排除であり、問題699全体の完全証明ではない。**
Lean による追加検証は行わない。

## 1. 使用する必要条件

反例を仮定し、n=2^u M、M は正の奇数と書く。既存の有限証明書と
`i3_2adic_and_square_continuation.md` により

\[
u\ge43,\qquad M^3<2^{u-2},\qquad
v=v_2(j)\le\lfloor u/4\rfloor,\qquad 4\le j<n/2.
\tag{1}
\]

最後の v の条件は j が奇数のとき v=0 と解釈する。
さらに

\[
g=\begin{cases}3&v_3(M)=1,\\1&v_3(M)\ne1,\end{cases}
\qquad T=M/g
\]

とすると T|j である。実際、p^e が M の完全素数冪で、p≠3 または
e≥2 なら p は binom(n,3) を割る。反例では p は binom(n,j) を割れない。
Kummer の無繰り上がり条件を p^e で見ると n≡0 なので j≡0 となる。
p=3,e=1 の場合だけを除くと、ちょうど T|j が得られる。

以下の補題自体は有限証明書を使わず、u≥16 で成り立つ。

## 2. 一般の平方排除補題

**補題。** u≥16、M は正の奇数、M³<2^(u−2)、g∈{1,3}、g|M、T=M/g とする。
n=2^u M、4≤j<n/2、T|j、v₂(j)≤u/4 を満たすなら

\[
L=\frac{j(n-j)}{n-1}
\]

は整数の平方ではない。

**証明。** L=ℓ²、ℓ>0 と仮定する。j>1 より L<j、従って ℓ<j。
また L<(n+1)/4、L は整数、4|n なので

\[
\ell\le\sqrt n/2.
\tag{2}
\]

T|j かつ T|n より T²|ℓ²(n−1)。gcd(T,n−1)=1 だから T²|ℓ²、従って

\[
T\mid\ell.
\tag{3}
\]

v=v₂(j)<u なので v₂(n−j)=v、従って v₂(ℓ)=v。
正の整数 λ=j−ℓ²=j(j−1)/(n−1) に対して

\[
(j-\ell)(j+\ell)=n\lambda.
\tag{4}
\]

j=2^v J、ℓ=2^v E と書くと J,E は正の奇数、J>E。
J−E,J+E の一方の2進指数はちょうど1。
v≥1 なら v₂(λ)=v なので、(4) より j−ℓ,j+ℓ の他方の2進指数は u−1。
v=0 なら v₂(λ)≥1 なので、他方は少なくとも u。
いずれの場合も、ある σ∈{−1,1} に対して 2^(u−1)|j+σℓ。
(3) と合わせ、ある正の整数 r に対して

\[
j+\sigma\ell=rT2^{u-1}=\frac{rn}{2g}.
\tag{5}
\]

(2) より r<g(1+1/√n)<g+1、従って 1≤r≤g。
つまり M がどれほど大きくても、この候補数は三つ以下である。

### r=g の場合

(5) は j+σℓ=n/2。j<n/2 なので σ=1。
ℓ²(n−1)=j(n−j) に代入すると ℓ²=n/4。
従って 2v=u−2 となり、v≤u/4 と矛盾する（u>4）。

g=1 の場合はこれで全て終了する。残るのは g=3、r=1,2 だけである。

### g=3、r=1,2 の場合

(5) を ℓ²(n−1)=j(n−j) に代入して平方完成すると

\[
(6\ell+\sigma(3-r))^2=r(6-r)n+(3-r)^2.
\tag{6}
\]

一方、M³<2^(u−2)、u≥16 より

\[
n^3=2^{3u}M^3<2^{4u-2}\le 2^{6u-30},
\quad\text{従って}\quad\sqrt n<2^{u-5}.
\tag{7}
\]

r=1 なら z=3ℓ+σ>0 として

\[
z^2-1=5n/4=15T2^{u-2}.
\]

z は奇数であり、z−1,z+1 の一方の2進指数が1、他方が u−3。
従って

\[
2^{u-3}\le z+1\le3\ell+2\le\frac32\sqrt n+2
<3\cdot2^{u-6}+2<2^{u-3},
\]

矛盾する。

r=2 なら z=6ℓ+σ>0 として

\[
z^2-1=8n=3T2^{u+3}.
\]

同じ理由で z−1,z+1 の一方の2進指数が u+2 だから

\[
2^{u+2}\le z+1\le6\ell+2\le3\sqrt n+2
<3\cdot2^{u-5}+2<2^{u+2},
\]

再び矛盾する。全ての候補が排除された。□

## 3. 反例への帰結

(1) を満たす全ての反例に補題を適用できる。従って

\[
\boxed{\text{i=3 の反例では、全ての奇数 M について }
\frac{j(n-j)}{n-1}\text{ は整数の平方ではない。}}
\tag{8}
\]

M=1,3 の分類を個別に繰り返す必要はなくなった。
これは M の有限範囲での探索からの推測ではない。

さらに δ=3 if v₃(n−1)=1 else1 と置く。
第1合同条件により L*=δj(n−j)/(n−1) は正の整数。
n≡0 (mod 2^u) から L*≡δj² (mod 2^u) であり、v≤u/4、u≥43 より
v₂(L*)=2v、L*/2^(2v)≡δ (mod 8)。
L*=d t²、d は平方因子をもたない正の整数と書くと、d は奇数で d≡δ (mod 8)。
(8) によって δ=1 なら d=1、δ=3 なら d=3 を除外できる。従って、
**全ての M について**

\[
\boxed{\delta=1\Rightarrow d\ge17,\qquad
\delta=3\Rightarrow d\ge11.}
\tag{9}
\]

## 4. 平方でない枝で試した整数方程式

ここでは第1条件の分母の3の例外がなく L=j(n−j)/(n−1) が整数、
第2条件も Q=n/2−1 を丸ごと使える場合を記す。
この場合、Q|j(n/2−j)(n−j) と gcd(Q,n−1)=1 から

\[
H=\frac{L(n/2-j)}{n/2-1}\in\mathbb Z,\qquad 0<H<L.
\]

q=j−L、q'=n−j−L とすると

\[
qq'=L(L-1),\qquad n=q+q'+2L,
\quad H(q+q'+2L-2)=L(q'-q).
\]

q' を消去すれば

\[
(L+H)q^2+2H(L-1)q-(L-H)L(L-1)=0.
\tag{10}
\]

従って、正の整数 R=(L+H)q+H(L−1) に対し

\[
\boxed{R^2=(L-1)(L^3-H^2).}
\tag{11}
\]

これは平方でない L に対しても必要な整数条件である。
しかし (10) の他方の根は −(L−H)q'/(L+H)<0 で、一般には整数でもない。
そのまま Vieta の根の交換を使って正の小さい反例へ降下することはできない。
例えば n=76672、j=26775 は二つの合同条件を満たし、
L=17425、H=5255、q=9350、q'=32472 となるが、他方の根は整数ではない。
この例は n の反例必要条件を満たさず、問題699の反例ではない。

(11) から L の平方性を導くこともできない。
現時点では (9) の d の上限も、平方でない枝を排除する降下も得られていない。
したがって i=3 の一般の場合と 4≤i≤204 は未解決のままである。

## 5. 検算

`audit_i3_all_square.py` は、平方完成と (10)–(11) を記号計算で検査し、
u=4..20、M³<2^(u−2) の範囲を平方根から独立に列挙する。
この有限列挙は補題の証明ではなく、全 u に対する証明は第2節で与えた。
結果は `verification_i3_all_square.json` に保存する。
