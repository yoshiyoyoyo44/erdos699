# 2026-09-20 添付の即時統合と一様な条件への継続

**部分的成果。i=3 全体、問題699全体の完全解決ではない。**

**統合後の進展：** [混合した差の続稿](i3_mixed_parameter_bound_2026-09-20.md)で、
D=w−4λ に対して n<18D²/(δ₁χᵥT)+21 を証明し、固定Dの明示的な有限還元を実装した。

## 1. 原本と取り込み範囲

[添付の原本](source_progress_handoff_2026-09-20.md)をバイトを変えず保存した。
SHA-256 は `237c08e3b45eac55c4fe65ad63e347893195d7c7f7a148466512866038018cc5`。
統合前の HEAD は `ad08b6fa9e47ff49f58895cfee328a0d6c0eb985`。

依頼に従って大規模な再探索は行わず、既存の定義への短い代入で分かる事項だけを整理した。
原本は研究上の入力であり、その全主張を証明済みと扱うものではない。

* 次数3の分類は既存の[証明](i3_cubic_digit_classification_2026-09-20.md)に接続する。
* 次数4〜6の新しい族・排除・有理パラメータ化は、原本に式と証明が収録されていない。
  報告された進捗として保存し、今回の定理の前提には使わない。
* balanced (2,2,2) の緩和した連続族について、原本が述べる撤回を引き継ぐ。
* 過去の数値上界に関する原本の一般的な注意だけから、特定の既存証明の有効・無効は判断しない。
  今回の議論はその数値上界を使わない。

## 2. すぐ接続できる整数恒等式

記号は[既存統合稿](i3_integrated_digits_and_center.md)に従う。
特に n=γT2^u、j=Tk、Q₁=(n−1)/δ₁、Q=(n/2−1)/δ₂=ABC、δ=δ₁δ₂、
j=TSBg、n−j=TRCh、j−1=RAa、n−j−1=SAb、s=T²gh とする。

q=δBC−Aab とおくと、既存の BCs−A²ab=δ₁ から

\[
Aq+BCs=\delta_1n/2,
\qquad BCs=\frac{\delta_1 j(n-j)}{n-1}.
\tag{1}
\]

したがって q>0。v=v₂(j)、u−1>2v とし、g=2^v g₀、h=2^v h₀ と書けば

\[
q=2^{2v}Tq_0,\quad q_0\text{ は正の奇数},\quad (T,q_0)=1,
\]
\[
Aq_0+BCTg_0h_0=\delta_1\gamma2^{u-1-2v}.
\tag{2}
\]

証明は (1) の両辺の付値を比べるだけである。2 では BCs の付値が2v、
p^e∥T では n/2 の付値がe、BCs の付値が少なくとも2eとなる。
u−1>2v は既存の反例の条件から従い、奇数 j も v=0 として含む。

N₁=ab+δ₁δ₂²B²C² に対しても

\[
AN_1+q=\delta BC(n/2),\quad N_1=2^{2v}TN_0,
\quad N_0\text{ は正の奇数},\quad (T,N_0)=1.
\tag{3}
\]

T の正確な付値は既存統合稿第3.4節の N と同じ結果である。
(q,BC)=1 なので (3) から gcd(q₀,N₀)∣3 も従う。
これらは便利な正規化であるが、二つの独立した追加条件と数えてはいけない。

添付の高精度の二式も既存の表示から直接得られる。

\[
\delta_1R^2Ch-Bg=AbH,\qquad
\delta_1S^2Bg-Ch=AaH,\qquad H=\gamma2^u.
\tag{4}
\]

例えば第一式に S を掛けると、左辺は (n−1)(H−k)−k=H(n−j−1)=HSAb。
従って法2^(u−v)の合同式は正しいが、それだけでは独立した排除は増えない。

## 3. 中心と添字の量

ρ=j(j−1)/(2Q₁)、ρ(ρ−δ₁)=λQ とする。D=j(j−1) と置き、
4λQQ₁²=D(D−2δ₁Q₁) を n/2 を法として読むと

\[
4\lambda\equiv-\delta_1^2\delta_2D(D+2)\pmod{n/2}.
\]

v≥2 で v₂(λ)=v−1 となることは、v₂(ρ)=v−1 と ρ−δ₁ が奇数であることからも直ちに従う。
E=(ρ−δ₁)/C は正整数で λ=r_C E、gcd(r_C,E)∣δ₁。
ここでは C 上で j≡2 より ρ≡δ₁ であることを使う。

中心の整数を Z、**R₀=2ρ−δ₁ と符号を固定**すると

\[
(w-4\lambda)Q=(Z-R_0)(Z+R_0),\quad
AC\mid Z-R_0,\quad B\mid Z+R_0.
\]

従って u₀=(Z−R₀)/(AC)、v₀=(Z+R₀)/B は整数で u₀v₀=w−4λ。
u₀ の正値は主張しない。w と λ が共に動くため、この表示だけで有限性は出ない。

## 4. 三次成分の付値の小修正

原本第6節の X₀,X₁,X₂,X₃ は正整数で、和は2δn。
偶数 j に対する記述のうち、v₂(X₀)=v₂(X₃)=v+1 には **v≥2** が必要。
v=1 では正しくは

\[
v_2(X_3)=1+v_2(j-2),\qquad
v_2(X_0)=1+v_2(n-j-2).
\]

両方とも少なくとも3であり、一般に等しいとは限らない。
v₂(X₁)=v₂(X₂)=2v、および交代和の v₂(K)=v+2 は偶数 j 全体で成立する。
K から正の下界を導く際は K>0 を確認する。
これは既存の中心の四乗下界から従うが、K の付値だけから符号は決まらない。

## 5. 次の研究対象

(2) は全ての反例候補を、小さい n′=δ₁γ2^(u−1−2v) と奇数
j′=BCTg₀h₀ の組へ送る。しかし j′≤n′/2、二つの整除条件、
全桁の Kummer 条件を保存する証明がそろって初めて反例の降下となる。

次は有限次数の分類を増やすより、この変換の保存条件を明示し、
成立する条件と追加の証明が必要な条件を切り分ける。
単に指数が減ることを完全解決とは扱わない。
