# i=3、n が2の冪の場合の平方の枝

2026-09-12。問題699全体、あるいは i=3 全体の証明ではない。

## 命題

n=2^u、u≥3、4≤j<n/2 とする。
\[
L=\frac{j(n-j)}{n-1}
\]
が整数の平方なら、j は問題699の i=3 における反例を与えない。

## 証明

L=ℓ²、ℓ>0 と仮定する。j>1 なので L<j、従って 0<ℓ<j。
また
\[
\lambda=j-L=\frac{j(j-1)}{n-1}\in\mathbb Z_{>0},\qquad
(j-\ell)(j+\ell)=n\lambda.
\tag{1}
\]
j=2^vJ、J 奇数と置く。j<n/2 より v≤u−2。
n−j の2進指数も v であり n−1 は奇数なので、L の定義から v₂(ℓ)=v。
ℓ=2^vE、E 奇数と書ける。

まず v=0 とする。j は奇数なので λ の2進指数は v₂(j−1)≥1。
j−ℓ と j+ℓ は正の偶数で、その片方の2進指数はちょうど1。
(1) より他方は2^uの倍数である。しかし両方とも0より大きく n 未満なので矛盾。

次に v≥1 とする。このとき v₂(λ)=v₂(j)=v。
(1) を2^(2v)で割ると
\[
v_2((J-E)(J+E))=u-v.
\]
J,E は奇数だから、一方の因子の2進指数は1、他方は u−v−1。
従って j−ℓ または j+ℓ は2^(u−1)=n/2の倍数になる。
0<j−ℓ<n/2、かつ0<j+ℓ<2j<nなので、必ず
\[
j+\ell=n/2.
\]
これを ℓ²(n−1)=j(n−j) に代入すると ℓ²=n/4。
ゆえに u は偶数であり、t=2^(u/2−1) と置けば
\[
n=4t^2,\qquad j=2t^2-t,\qquad L=t^2.
\tag{2}
\]

反例を仮定する。u は偶数なので n/2−1=2^(u−1)−1 は3で割れない。
この奇数を Q とする。Q の各完全素数冪に Kummer 条件を適用すると
\[
Q\mid j(n/2-j)(n-j).
\tag{3}
\]
ところが (2) より右辺は t³(n−1) である。
gcd(Q,t)=1、gcd(Q,n−1)=1なので (3) は Q=1 を強制する。
これは n≥8 に反する。□

## この命題で埋まらない部分

最初の整除条件から L が平方であるとはいえない。
例えば n=512、j=147 では
\[
\frac{j(j-1)}{n-1}=42,\qquad L=105
\]
であり、L は平方ではない。この例は第2条件に反するので反例ではないが、
「最初の整数条件から平方が従う」という推論を退ける。

対称デザインの実在を仮定すれば平方条件を得る理論があるが、
この数論的な反例仮定からそのようなデザインの実在は証明されていない。
したがって、デザインの分類結果をそのまま適用して残りを排除することはしない。
