# Erdős 699：二つの新資料の統合と、中心の整数による継続

2026-09-13。**部分的成果であり、i=3 全体や問題699の完全証明ではない。**
二つの添付資料を既存の証明と照合し、そこから得た一般化と新しい還元を記録する。
文献上の新規性、Lean による形式化、未実行の整数点の完全決定は主張しない。

## 1. 取り込んだ資料と今回の到達点

元の添付をバイトを変えず保存した。

* [方向2の進展](source_direction2_progress_2026-09-13.md)：三つの比の圧縮、
  緩和した方程式の無限族、T の単約数条件。
* [Kummer 全桁の進展](source_kummer_all_digits_progress_2026-09-13.md)：
  完全素数冪を除いた商への移行、符号付き桁集合、T=1 での添字と中心距離の下界。

添付内の記述は検証対象の数学的主張として読み、作業の指示とは区別した。
統合前のリポジトリ HEAD は `00f83b7b0c25671ad50e13bea096480cfe97cf2b`。
前段の証明は [非平方の統合稿](i3_nonsquare_merged_continuation.md) と
[境界・固定因子の稿](i3_boundary_and_fixed_blocks.md) を参照する。

今回そこから進めた内容は次の通り。

1. 全桁条件の対称な表現と添字の下界を、T=1 に限らず一般の T に拡張した。
2. 中心距離 c=n/2−j について、一般の T の三乗下界に加え、より強い四乗下界を得た。
3. 正の奇数 w を導入し、既存の三因子との積表示と、T の新しい単約数条件を証明した。
4. 任意の固定 W₀ に対し、w≤W₀ の反例候補は有限個であることを、
   有限個の非特異な Mordell 曲線への還元と Siegel の定理から示した。

主要分岐 δ₁=δ₂=1 では、添付の c>Q^(2/3) に対して

\[
\boxed{j>2^{5/4}T^{1/4}Q^{3/4},\qquad c>61^{1/4}Q^{3/4}.}
\tag{1}
\]

この二つの下界は、大きい n に対して同時に成立し得る。矛盾や完全解決とはいえない。

## 2. 全稿で共通に使う記号

i=3 の反例を仮定する。既存の結果により

\[
n=2^uM,\quad u\ge49,\quad M>0\text{ は奇数},\quad M^3<2^{u-2}.
\]

γ=3 if v₃(M)=1、そうでなければ γ=1 とし、

\[
T=M/\gamma,\quad H=\gamma2^u,\quad n=HT,\quad j=Tk,
\quad 4\le j<n/2.
\]

δ₁,δ₂ は n−1,n/2−1 の3進指数がちょうど1なら3、そうでなければ1。

\[
Q_1=(n-1)/\delta_1=RS,\quad
Q=Q_2=(n/2-1)/\delta_2=ABC,\quad \delta=\delta_1\delta_2.
\tag{2}
\]

可能な (γ,δ₁,δ₂) は (1,1,1),(1,1,3),(1,3,1),(3,1,1)。
Q₁,Q,T は互いに素な奇数で、gcd(Q,δ₁δ₂)=gcd(T,γδ₁δ₂)=1。
完全素数冪を、j の剰余が1,0となる R,S、および1,0,2となる A,B,C に分ける。

既存の正の整数 a,b,g,h と s,r_B,r_C は

\[
k=SBg,\quad j-1=RAa,\quad H-k=RCh,\quad n-j-1=SAb,
\]
\[
s=T^2gh,\quad r_B=Tbh/2,\quad r_C=Tag/2,
\quad BCs-A^2ab=\delta_1
\tag{3}
\]

で定義され、r_B,r_C は T の倍数となる正整数である。
初等的に δA>2s と A>1 が得られている。
δ₁=3 なら 3∤A なので A≥5。これらは今回使うが、計算を伴う A,B,C≥11 は
今回の新しい補題の証明には必要ない。

中心は一貫して

\[
c=n/2-j>0,\qquad \Delta=2c
\]

と書く。添付第2稿の B=n/2 と M=Q/q は既存の B,M と重複するため用いない。
後で導入する w は中心から定まる新しい整数であり、旧稿の2進指数の記号 w とは別物である。

## 3. 方向2の結果の照合

### 3.1 三つの既約分数

x=s/A, y=r_B/B, z=r_C/C, ε=δ₁/(ABC) と置くと

\[
x+y+z=\delta+\varepsilon,\qquad 4yz=x(x-\varepsilon).
\tag{4}
\]

第1式は既存の比の和、第2式は 4r_Br_C=sab と (3) から直接従う。
また

\[
\gcd(s,A)=\gcd(r_B,B)=\gcd(r_C,C)=1.
\tag{5}
\]

最初は (3) から従う。残りは既存の等式

\[
\frac{r_B}{\delta B}=\frac{(n-j)(n-j-1)}{(n-1)(n-2)},\qquad
\frac{r_C}{\delta C}=\frac{j(j-1)}{(n-1)(n-2)}
\]

で各素数の指数を比較する。p|B なら n≡2,j≡0 (mod p) なので第1分子は単元、
分母の p 進指数は v_p(B) に等しい。p|C でも第2分子は単元で同様。
gcd(B,g)=1 や gcd(C,h)=1 を仮定する証明ではない。

### 3.2 緩和した方程式には、三因子が全て大きくなる無限族がある

m≥0 に対して

\[
\begin{aligned}
A&=18m+11,&B&=144m+85,&C&=72m+41,\\
a&=2,&b&=128m+72,&g&=8m+5,&h&=1,\\
S&=108m+63,&R&=3456m^2+4104m+1217,\\
n&=2ABC+2,&j&=SBg
\end{aligned}
\tag{6}
\]

とすると、T=δ₁=δ₂=1 とした (3)、RS=n−1、および全ての低位の剰余分割が成立する。
各恒等式は多項式として検算できる。A と B=8A−3、C=4A−3 は互いに素であり、
B−2C=3 と 3∤ABC から B,C も互いに素である。
また

\[
c=(18m+11)(3456m^2+3816m+1051)>0,\quad n\equiv1\pmod9,
\quad \min(A,B,C)=18m+11\longrightarrow\infty.
\]

さらに n/4=P(m)=93312m³+165240m²+97497m+19168 は
P(m)≡m (mod 2), P′(m)≡1 (mod 2) を満たす。
根 m mod 2^r の二つの持ち上げ m,m+2^r のうち、ちょうど一つが mod 2^(r+1) の根になる。
従って任意の r に対し v₂(n)≥r+2 となる m があり、その合同類で m は任意に大きくできる。

よって、これらの緩和した恒等式・正値性・高い v₂(n) だけから
min(A,B,C) の一様な上限を出す方針には明確な障害がある。
**この族は M³<2^(u−2)、T|j を含む本来の正規化を満たす族ではない。**

### 3.3 この無限族が元の反例にならない理由

常に gcd(n,j)=1。j は奇数であり、奇素数 p が n,j を割ると仮定すると、
j−1=2AR から p∤AR、n−j=RC から p|C。しかし n=2ABC+2≡2 (mod p) に矛盾する。

本来の正規化には T|gcd(n,j) が必要なので、この族が反例なら T=1。
n≡1 (mod 9) から n=3·2^u はなく、n=2^u, u≡0 (mod 6) が必要になる。
このとき u−1 は奇数なので、p|2^(u−1)−1 の全ての素数について ord_p(2) は奇数。
従って (2/p)=1、すなわち p≡±1 (mod 8)。その約数 B も ±1 (mod 8) のはずだが、
B≡5 (mod 8) に矛盾する。

さらに、n は2の冪でなく 3∤n なので、必ずある素数 p≥5 が n を割る。
gcd(n,j)=1 により j の最下位 p 進桁は n の0という桁を超える。
この p は実際に二つの二項係数の共通素因数となる。族全体を排除する議論である。

### 3.4 添付の単約数条件と大きさ

D=δ₂BC、W=γ2^(u−1) とすると DA+1=TW。
(3) を D² 倍し、A²D²=(TW−1)² を代入すると

\[
N:=ab+\delta_1D^2\equiv2TWab\pmod{T^2}.
\]

gcd(T,ab)=1 は (3) を mod T で見れば従う。
gcd(T,2W)=1 も正規化から従うため

\[
\boxed{T\mid N,\qquad \gcd(T,N/T)=1.}
\tag{7}
\]

つまり T は N の単約数である。(3) から gcd(ab,BC)=1 も得られる。
δA>2s を使うと

\[
ab<\frac{\delta BC}{2A},\qquad
\min(A,B,C)>T/3.
\tag{8}
\]

第2式は既存の A>2T²/δ、B≥(T+1)/δ、C>4T/δ、δ≤3 を合わせたもの。
添付の一様上限に関する結論は条件付きのままであり、上限自体は証明していない。

## 4. 全桁条件の統合と一般の T への拡張

### 4.1 商への移行と符号付き桁集合

p≥3 を素数、q=p^e, e≥1 とする。n=qN+r, j=qJ+s、0≤s≤r<p なら

\[
v_p\binom nj=v_p\binom NJ.
\tag{9}
\]

実際、j+(n−j) の下位 e 桁に繰り上がりはなく、上位の繰り上がりは
J+(N−J) のものと一致する。
使用する Kummer の定理については
[Bergman, §4, Lemma 5](https://math.berkeley.edu/~gbergman/papers/nomial.pdf) を参照。

N=Σn_νp^ν とし

\[
\mathcal D_p(N)=\left\{\sum d_\nu p^\nu:
d_\nu=-n_\nu,-n_\nu+2,\ldots,n_\nu\right\}
\]

と定義する。無繰り上がり条件 j_ν≤n_ν に対し d_ν=n_ν−2j_ν と置けば

\[
p\nmid\binom NJ\quad\Longleftrightarrow\quad N-2J\in\mathcal D_p(N).
\tag{10}
\]

逆向きも j_ν=(n_ν−d_ν)/2 で戻る。これは必要条件だけでなく、この一素数についての同値である。

### 4.2 共通の中心偏差による六ブロックの表

各ブロックに属する完全素数冪 q=p^e について、残る全桁条件は次の通り。

| ブロック | j mod q | N | D=N−2J が属すべき集合 |
|---|---|---|---|
| T | 0 | n/q | Δ/q ∈ 𝒟_p(N) |
| R | 1 | (n−1)/q | (Δ+1)/q ∈ 𝒟_p(N) |
| S | 0 | (n−1)/q | (Δ−1)/q ∈ 𝒟_p(N) |
| A | 1 | (n−2)/q | Δ/q ∈ 𝒟_p(N) |
| B | 0 | (n−2)/q | (Δ−2)/q ∈ 𝒟_p(N) |
| C | 2 | (n−2)/q | (Δ+2)/q ∈ 𝒟_p(N) |

従来の低位条件は左側の商が整数になる部分を述べていた。
上位の全ての桁では集合への所属が必要になる。
3が二項係数から消える場合は γ,δ₁,δ₂ の定義でブロックから除いている。

添付の T=1 の対称形も **全ての T に対して成立する**。
q|Q に対し Q̃=Q/q、s=j mod q∈{0,1,2}、X=(c−(1−s))/q と置けば

\[
N=2\delta_2\widetilde Q,\quad J=\delta_2\widetilde Q-X,\quad
2X\in\mathcal D_p(2\delta_2\widetilde Q).
\tag{11}
\]

q|Q₁ なら Q̃₁=Q₁/q、s∈{0,1}、σ=1−2s、X=(2c−σ)/q として

\[
N=\delta_1\widetilde Q_1,\quad J=(\delta_1\widetilde Q_1-X)/2,\quad
X\in\mathcal D_p(\delta_1\widetilde Q_1).
\tag{12}
\]

導出に T=1 は使っていない。ただし、どの剰余 s を取ったかは X の定義に残る。
ブロック分割の情報が全て不要になるという意味ではない。

### 4.3 最小の正の偏差と、0の場合の区別

N>0 とする。奇数の桁を持つ最大の位置を h とすると

\[
\min(\mathcal D_p(N)\cap\mathbb Z_{>0})
=p^h-(N\bmod p^h)=:g_p(N).
\tag{13}
\]

h より上の桁を0、h の桁を1、下を全て −n_ν とすれば右辺を達成する。
最高の非零桁が h ならこれより小さくできず、h より上ならその桁は偶数なので
正値には少なくとも2が必要になり、下を最大限引いても p^h より大きい。
最高の非零桁が負なら全体も負になる。

全ての桁が偶数なら 0 が許される。最小の正値は一般には 2p^{v_p(N)} であり、
完全素数冪を除いた商のように p∤N なら2となる。
したがって g_p(N) の下界を使う際は **D>0 を先に確認する**。
例えば B の Δ−2 は中心直近で0になる可能性があり、0を正の最小値と比較してはいけない。

### 4.4 添付の二つの全桁排除を再検算

(n,j)=(76672,26775) では、p=17 の商は (N,J)=(4510,1575)。
次の桁が N mod 17=5, J mod 17=11 なので失敗する。
p=41 では (N,J)=(1870,653) で D=564<g₄₁(1870)=1492。
厳密な階乗の素数指数で直接調べても、二項係数の17進指数は1、41進指数は2。
第3.3節の n の奇素因数599も、二つの二項係数を割る。

## 5. 添字の圧縮を全ての T に拡張する

\[
\rho=\frac{j(j-1)}{2Q_1}=ABr_C
\tag{14}
\]

は正整数で T|ρ。δ₁Q₁=2δ₂Q+1 と低位条件 Q|j(j−1)(j−2) から

\[
4\rho(\rho-\delta_1)
\equiv\delta_1^2j(j-1)(j-2)(j+1)\equiv0\pmod Q.
\]

よって Q|ρ(ρ−δ₁)。ここで **商の正値性も必要** である。
A>1 と (14) から、δ₁=1 なら ρ≥A≥3、δ₁=3 なら ρ≥A≥5。
従って ρ>δ₁ である。
ρ と ρ−δ₁ は異なる偶奇を持ち、Q は奇数、gcd(Q,T)=1 なので

\[
\boxed{\frac{\rho(\rho-\delta_1)}Q\in2T\mathbb Z_{>0},\qquad
\rho(\rho-\delta_1)\ge2TQ.}
\tag{15}
\]

j²>j(j−1)=2Q₁ρ を平方し、ρ²>2TQ を使って

\[
\boxed{j^4>8TQ Q_1^2.}
\tag{16}
\]

δ₁=δ₂=1 なら Q₁=2Q+1>2Q であり、(1) の添字の下界が得られる。

## 6. 中心距離について、まず全 T の三乗下界を得る

低位の剰余条件を c=n/2−j に移すと

\[
T\mid c,\qquad Q_1\mid4c^2-1,\qquad Q\mid c(c^2-1).
\tag{17}
\]

実際、Q₁ 上では 2c≡1−2j、Q 上では c≡1−j である。
c=1 なら Q₁|3 となり u≥49 に反するので、以下は正整数である。

\[
r=\frac{4c^2-1}{Q_1},\qquad \tau=\frac{c(c^2-1)}Q.
\]

r は奇数で、直接消去すると

\[
2Q(2\delta_1\tau-\delta_2cr)=c(r-3\delta_1).
\tag{18}
\]

**r>3δ₁ を示す。** δ₁=1 で r=1 なら (18) から Q|c。
0<c<δ₂Q なので δ₂=1 は不可能。
δ₂=3 でも c=Q,2Q に限られ、4c²=6Q+2 と Q≥3 に矛盾する。
δ₁=3 なら δ₂=1,3∤Q。r=1,3,5,7 ではそれぞれ Q|4c,3c,2c,c となり、
gcd(Q,6)=1 と 0<c<Q に矛盾する。
r=3δ₁ の場合は、どの分岐も 2(c²−1)=3δ₂Q となり偶奇が矛盾する。

(18) の左括弧は正整数で、T|c, T|τ から T の倍数。
r<2δ₁c²/(δ₂Q) と合わせると

\[
2QT\le c(r-3\delta_1)<cr<\frac{2\delta_1c^3}{\delta_2Q},
\]

従って

\[
\boxed{c^3>\frac{\delta_2}{\delta_1}TQ^2.}
\tag{19}
\]

## 7. 新しい中心の整数 w と四乗下界

(17) より r≡δ₁(4c²−1) (mod Q)。従って

\[
(r+\delta_1)(r-3\delta_1)
\equiv16\delta_1^2c^2(c^2-1)\equiv0\pmod Q.
\]

また n≡0 (mod 4) から r≡δ₁ (mod 4)。二つの因子はともに2進指数が1である。
前節の r>3δ₁ により

\[
\boxed{w=\frac{(r+\delta_1)(r-3\delta_1)}{4Q}
\text{ は正の奇数}.}
\tag{20}
\]

z_c=(r−δ₁)/2 と置くと z_c は正の偶数であり、平方完成と rQ₁=4c²−1 から

\[
\boxed{z_c^2=\delta_1^2+wQ,\qquad
4c^2=n+2Q_1z_c.}
\tag{21}
\]

z_c>√(wQ) と n>0 を使えば

\[
\boxed{4c^4>wQ Q_1^2.}
\tag{22}
\]

### 7.1 既存の三因子との正確な対応

\[
e_c=\delta A-2s,\qquad f_c=\delta BC-2Aab
\]

と置く。e_c は既存の δA>2s により正の奇数。
(3) から

\[
Af_c-BCe_c=2\delta_1,
\]

なので f_c も正の奇数である。
4c²−1=(n−1)(n+1)−4j(n−j) を使うと

\[
r-3\delta_1=2BCe_c,\qquad r+\delta_1=2Af_c.
\]

従って、新しい整数は既存の量の積としても書ける。

\[
\boxed{w=e_cf_c,\qquad Af_c-BCe_c=2\delta_1,
\qquad\gcd(e_c,f_c)\mid\delta_1.}
\tag{23}
\]

特に δ₁=1 では二つの因子は互いに素。
緩和例 (76672,26775) は e_c=1,f_c=317,w=317 を与える。
この積表示は、(20) が三因子の議論と無関係に導入された量ではないことを示す。

## 8. T は w−δ₁²δ₂ の単約数になる

(21) の第2式、T|c,n、gcd(T,2Q₁)=1 から T|z_c。
z_c=TZ、W=γ2^(u−1) と書く。ここで

\[
Q=(TW-1)/\delta_2
\]

であることに注意する。分母に余分な2はない。
(21) の第1式を整理すると

\[
w-\delta_1^2\delta_2=T(wW-\delta_2TZ^2).
\tag{24}
\]

特に w≡δ₁²δ₂ (mod T)、従って gcd(T,w)=1。
gcd(T,W)=1 なので (24) を T で割った商も T と互いに素である。

\[
\boxed{T\mid w-\delta_1^2\delta_2,\qquad
\gcd\left(T,\frac{w-\delta_1^2\delta_2}{T}\right)=1.}
\tag{25}
\]

次節では w>δ₁²δ₂ および w≡δ₁²δ₂ (mod 4) も示す。T は奇数なので、その後は

\[
\boxed{w\ge\delta_1^2\delta_2+4T,\qquad
T\le\frac{w-\delta_1^2\delta_2}{4}.}
\tag{26}
\]

この関係は、添付第1稿の ab+δ₁δ₂²B²C² に対する単約数条件とは別の追加条件である。

## 9. 退化する値の全 M 排除と、小さい w の一様排除

### 9.1 w=δ₁²δ₂ は全ての M で不可能

この等式を仮定すると、(21) から z_c²=δ₁²n/2。
X=z_c/δ₁ は整数で n=2X²。u≥49 なので4|X。
さらに (21) の第2式は

\[
4c^2=2X(2X-1)(X+1).
\tag{27}
\]

gcd(2X−1,2X)=1、gcd(2X−1,X+1) は3の約数である。
従って 2X−1 の3以外の素因数の指数は全て偶数であり、2X−1 は奇数の平方、
または奇数の平方の3倍になる。しかし4|X より 2X−1≡7 (mod 8) であり、
平方の1、3倍の3のどちらとも一致しない。矛盾。

これは T=1 に限定した議論ではなく、奇数部分 M の大きさを使わない排除である。

### 9.2 二つの平方の式を同時に使うと、四乗の合同条件になる

K=n/2 とする。gcd(K,δ₁δ₂)=1 である。
(21) の第2式を2で割ると 2c²=K+Q₁z_c。
Q₁≡−δ₁⁻¹ (mod K) なので z_c≡−2δ₁c² (mod K)。
これを第1式と Q≡−δ₂⁻¹ (mod K) に入れると

\[
\boxed{w\equiv\delta_1^2\delta_2(1-4c^4)\pmod{n/2}.}
\tag{28a}
\]

特に、2^(u−1) を法としても同じ合同式が成立する。
u に上限を置いた実験ではなく、全ての u≥49 に対する条件である。

c₀=δ₁²δ₂ と略記する。j と c は同じ偶奇を持つ。
奇数 c は c⁴≡1 (mod 16) なので w≡−3c₀ (mod 64)。
偶数 c では c⁴≡0 または16 (mod 256) なので
w≡c₀ または −63c₀ (mod 1024) となる。
第9.1節で w=c₀ は排除済みだから、次の下界が従う。

| (δ₁,δ₂) | c₀ | j が奇数の w の下界 | j が偶数の w の下界 |
|---|---|---|---|
| (1,1) | 1 | 61 | 961 |
| (1,3) | 3 | 55 | 835 |
| (3,1) | 9 | 37 | 457 |

例えば (1,1) の偶数 c では w≡1 または961 (mod 1024)。
1は排除済みで、1の合同類の次の正値は1025なので、w≥961 となる。
従って、偶奇を問わず全ての T に対して

\[
\boxed{w\ge\kappa(\delta_1,\delta_2),\qquad
\kappa(1,1)=61,\quad\kappa(1,3)=55,\quad\kappa(3,1)=37.}
\tag{28}
\]

これで w>δ₁²δ₂ も従う。(28a) より w≡c₀ (mod 4) だから (26) も正当化される。
j が偶数なら w≡c₀ (mod 64) も成立するので、さらに w≥c₀+64T となる。
(22) と合わせた最新の下界は

\[
\boxed{4c^4>
\max\{\kappa(\delta_1,\delta_2),\delta_1^2\delta_2+4T\}\,Q Q_1^2.}
\tag{29}
\]

δ₁=δ₂=1 では Q₁>2Q を使って (1) の c>61^(1/4)Q^(3/4) を得る。
j が偶数なら、表の961,835,457と c₀+64T を使った、より強い版も得られる。
これらは第6節の三乗下界と併用できる。

## 10. 新しい有限性定理：w を固定した範囲

**定理。任意の固定した正整数 W₀ に対し、w≤W₀ を満たす i=3 の反例候補は有限個。**

各 w を固定する。(25) と第9節から、T は正の整数 w−δ₁²δ₂ の奇数の単約数に限られる。
従って T の候補は有限個。γ,δ₁,δ₂ の選択も有限個である。

(21) を δ₂² 倍して Y=δ₂z_c とすると

\[
Y^2=w\gamma T\delta_2\,2^{u-1}
+\delta_2(\delta_1^2\delta_2-w).
\tag{30}
\]

u−1=3m+ε、ε∈{0,1,2}、t=2^m と書き、

\[
\alpha=w\gamma T\delta_2\,2^\varepsilon>0,\qquad
\beta=\delta_2(\delta_1^2\delta_2-w)<0
\]

と置けば Y²=αt³+β となる。
整数 X=αt,V=αY への写像により

\[
\boxed{V^2=X^3+\alpha^2\beta.}
\tag{31}
\]

α²β≠0 なので判別式 −432(α²β)² は0でなく、これは非特異な楕円曲線である。
各曲線の整数点は有限個であるという
[Siegel の定理（Silverman, Chapter IX）](https://www.math.ens.psl.eu/~obenoist/refs/Silverman.pdf)
を適用する。曲線自体も有限個しか現れない。

各点から t=X/α、u=3log₂(t)+ε+1 が決まる。
t が正の2の冪でない点、V/α が正の整数でない点は候補にならない。
残る点から n,z_c が決まり、(21) の第2式から正の c、従って j は高々一つに定まる。
最後に元の条件を満たすものを取る部分集合も有限である。w≤W₀ の有限和で定理が従う。

この証明は u に有限の探索上限を置いていない。一方、具体的な W₀ についての
整数点を全て計算したわけではなく、反例が0個であることまでは示していない。
本稿の還元では新たな Magma 計算を使用していない。

## 11. 完全解決へ残る問題と、方針の更新

添付の無限族により、特別な n の形と T|j を落としたまま方向2を進めることはできない。
一方、全桁条件は六ブロックに共通の中心偏差で記述でき、今回の w は同じ中心から定まり、
既存の三因子とも (23) で結び付く。

次に検討する条件は、この組合せになる。

\[
\begin{gathered}
\text{六ブロック全ての符号付き桁集合への所属},\\
w=e_cf_c,\quad Af_c-BCe_c=2\delta_1,\quad
T\parallel_{\rm unitary}(w-\delta_1^2\delta_2),\\
\delta_2ABC+1=\gamma T2^{u-1}.
\end{gathered}
\]

ここで最後から2番目は (25) の単約数条件を表す略記であり、新しい仮定ではない。
具体的な研究の分岐は次の通り。

* 異なる素数の全桁条件から w の一様な上限、または w が増える場合の矛盾を得る。
  各素数での最小偏差を個別に並べるだけでなく、共通の c への同時条件として使う。
* w=e_cf_c と単約数条件により、固定した小さい w の曲線の計算を組み立てる。
  これは有限範囲の整理にはなるが、それだけで無限に増える w は扱えない。
* n の特別な形と全桁条件を保存する降下、または別途 4≤i≤204 を縮める方針を続ける。

既存の固定因子の有限性と今回の定理を合わせると、仮に相異なる反例候補が無限個あるなら、
任意の定数以下にとどまるものは有限個であり、**min(A,B,C) と w はともに無限大へ向かう**。
ただし、これを両者の一様上限や、候補が全く存在しないという結論へ置き換えてはいけない。
有限個だけ残る大きい候補も含め、i=3 の一般の非平方の枝は未解決である。
4≤i≤204 の状態も今回変更していない。

## 12. 検算の内容と限界

```text
python audit_i3_integrated_digits.py
```

[検算器](audit_i3_integrated_digits.py) と
[結果](verification_i3_integrated_digits.json) を保存した。

* 25個の有理式・多項式の恒等式および合同式の分子の整除性を SymPy で検査。
* 二項係数そのものと階乗の素数指数を82,355組で照合。
* p=3,5,7,11,13、e=1,2,3 における商への移行を346,698組で検査。
* 符号付き集合と無繰り上がり条件の同値を227,250組、最小の正値を1,500組で検査。
* 緩和した無限族の最初の513例、Hensel の持ち上げ128段、三つの共通素因数の例を検査。
* 小さい w を排除する平方剰余表、単約数条件の合同式、Mordell 曲線への写像を検査。

無限族・全桁の補題・退化値の排除・有限性の根拠は本文の証明であり、有限検算からの外挿ではない。
緩和族の正例は T=1 とした代数の検査で、本来の正規化を満たす正例ではない。
一般の T の主張は記号的な議論で証明し、実在する T>1 の反例候補で検証したとは主張しない。
新しい整数点の完全性の計算や Lean の証明は行っていない。
