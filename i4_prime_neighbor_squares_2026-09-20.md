# i=4：n=(p+1)² の全ての素数 p を処理する

2026-09-20。i=4 全体の解決ではないが、次の族には素数の上限を仮定しない結果を得た。

**定理。** \(p\) は任意の素数、\(n=(p+1)^2\) とする。
全ての \(5\le j\le n/2\) に対して

\[
\gcd\left(\binom n4,\binom nj\right)
\]

は素数 \(q\ge5\) を因数に持つ。

この証明は [i=3 の素数冪の稿](i3_prime_neighbor_powers_2026-09-20.md)と同じ全桁条件を使うが、
i=3 の結果や以前の大規模な計算には依存しない。

## 1. 大きい素数を一括して除く

\(p\ge5\) とし、定理に反する \(j\) があると仮定する。
\(p\mid n-1\)、\(p\nmid24\) なので \(p\mid\binom n4\)。
従って \(p\nmid\binom nj\) である。

\[
n=p^2+2p+1<2p^2
\]

より、Kummer の全桁条件と \(j\le n/2\) から

\[
j\in\{p,p+1,2p,2p+1\}. \tag{1}
\]

他に許される桁列0,1は \(j\ge5\) に反する。

\(n\) は4の倍数で、平方数なので \(n-2\not\equiv0\pmod3\)。従って

\[
Q=\frac{n-2}{2}=\frac{p^2+2p-1}{2}
\]

は奇数で、全ての素因数が5以上である。
\(Q\) の各完全素数冪 \(q^e\) について、\(q\mid\binom n4\) だから、反例の仮定と Kummer の条件により

\[
j\bmod q^e\le n\bmod q^e=2.
\]

従って

\[
Q\mid j(j-1)(j-2). \tag{2}
\]

\(p^2\equiv-2p+1\pmod{n-2}\) を使って (1) の四通りを代入すると、次の一次式が得られる。

| \(j\) | \(j(j-1)(j-2)\) と法 \(n-2\) で合同な値 \(R\) |
|---|---|
| \(p\) | \(13p-5\) |
| \(p+1\) | \(4p-2\) |
| \(2p\) | \(68p-28\) |
| \(2p+1\) | \(38p-16\) |

これらは全て \(0<R\le68p-28\) を満たす。
一方、\(p\ge134\) では

\[
Q-(68p-28)=\frac{p(p-134)+55}{2}>0.
\]

従って \(0<R<Q\)。\(Q\mid n-2\) なので (2) なら \(Q\mid R\) となり、矛盾する。
これで全ての素数 \(p\ge134\) が除かれた。

## 2. 有限の残りの確認

\(p<134\) の素数は32個で、最大は131、最大の \(n\) は17,424。
\(p\ge5\) では全桁条件で残る四つの \(j\) を調べ、各々について共通素数 \(q\ge5\) を記録する。
\(p=3\) は \(n=16\)、\(j=5,6,7,8\) を直接調べる。
\(p=2\) は \(n=9\) で適格な \(j\) が存在しない。

[検証器](audit_i4_prime_neighbor_squares.py)は整数演算だけで動く。
32個の素数の完全な列挙、全ての適格な \(j\) での全桁条件の確認、
残った124組での二項係数と gcd の計算、共通素因数の試し割りを実行する。
全桁条件で排除する組も含め、79,736組の \((n,j)\) を被覆する。

```text
python -X utf8 audit_i4_prime_neighbor_squares.py
```

[検証結果と全ての共通素数](verification_i4_prime_neighbor_squares.json)を保存した。
四つの剰余の恒等式にも1,992組の補助検算を付けたが、
\(p\ge134\) の排除は第1節の代数的証明による。

本定理は n が平方である全ての場合を扱うものではなく、平方根から1を引いた数が素数である場合を扱う。
一般の i=4 には、依然として別の議論が必要である。
