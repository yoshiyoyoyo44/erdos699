# i=3：降下候補の正確な更新式と、上位桁の非保存

2026-09-20。[ChatGPT回答の統合](i3_chatgpt_uniform_integration_2026-09-20.md)の続き。
**問題699もi=3全体も未解決。** 今回は、降下を反復するために欠けている条件を特定する。
新たな反例や全指数の排除を主張するものではない。

## 1. 結果と適用範囲

最も条件を引き継ぎやすい T=δ₁=1、奇数jの枝を扱う。
既存の反例なら n=2^u または3·2^u、u≥49である。
以下の代数自体は、8∣n、jは奇数、元の二つの整除条件、Z>0があれば成立する。

\[
N=n/2,\qquad J=\frac{j(n-j)}{n-1},\qquad
b=\delta_2,\quad\varepsilon=\delta'_2,
\]

とする。εはN/2−1の3進付値がちょうど1なら3、それ以外は1。
変換後のδ′₁は旧δ₂と等しいのでδ′₁=b。

今回得た内容は次の三つである。

1. 変換後のZ′,ρ′,w′,λ′を元のn,Z,wで明示した。
2. 新しい第二の整除条件は、w′とλ′の**両方**の整数性と同値になる。
3. 引き継がれる素数でも、元の全桁Kummer条件から変換後の全桁条件は一般には従わない。
   元の二つの整除条件まで満たす具体例と、第一条件を満たす無限族で示した。

第3点の例は元の反例の全条件を満たしていない。
従って反例から反例への降下を論理的に否定したのではなく、
一つの素数や低位の整除条件だけによる保存の証明を否定したものである。

## 2. 変換後の中心と添字の商

旧記号は

\[
Z=\frac{(n-2j)^2-n}{2(n-1)},\qquad
Z^2=1+w\frac{n-2}{2b}.
\]

前稿の N−2J=Z と、新しい第一条件 Q′₁=(N−1)/b を使うと

\[
\boxed{Z'=\frac{b((N-2J)^2-N)}{2(N-1)}=\frac{w-b}{2}.}
\tag{1}
\]

旧反例の付値 v₂(w−b)=2 より、Z′は正整数でv₂(Z′)=1。
正値性には旧w>bを使う。

旧ブロックQ=ABCでは、jの剰余0,1,2がJの剰余0,1,0へ移る。
よってQ′₁∣J(J−1)。Jが奇数なので

\[
\rho'=\frac{bJ(J-1)}{2(N-1)}\in\mathbb Z.
\]

Z′=bN/2−2bJ+4ρ′とN−2J=Zから

\[
\boxed{\rho'=\frac{bn+2w-2b-4bZ}{16}.}
\tag{2}
\]

Q′₂=(n−4)/(4ε) とすると、新しいw′,λ′の候補は

\[
\boxed{w'=\frac{\varepsilon(w-3b)(w+b)}{n-4},}
\tag{3}
\]
\[
\boxed{\lambda'=\frac{4\varepsilon\rho'(\rho'-b)}{n-4}.}
\tag{4}
\]

これらは今の段階では**有理数**である。式が書けることと整数になることは別である。
特に(1),(2)は既存の条件から整数になるが、(3),(4)には新しい分母n−4が残る。

## 3. 第二の整除条件を正確に特徴付ける

**補題。** Nが4の倍数、Jは奇数、Q′₁∣J(J−1)、Z′,ρ′を上記の式で定める。
Q′₂は奇数でgcd(Q′₂,b)=1。すると

\[
\boxed{Q'_2\mid J(J-1)(J-2)
\quad\Longleftrightarrow\quad w',\lambda'\in\mathbb Z.}
\tag{5}
\]

**証明。** q=Q′₂、P=J(J−1)(J−2)とする。
N≡2 (mod q)、N−1≡1 (mod q)なので、奇数qでの2の逆元を用いて

\[
Z'\equiv b\{2(J-1)^2-1\},\qquad
\rho'\equiv bJ(J-1)/2\pmod q.
\]

従って

\[
Z'^2-b^2\equiv4b^2P(J-1),\qquad
\rho'(\rho'-b)\equiv\frac{b^2}{4}P(J+1)\pmod q.
\]

w′,λ′の整数性は、それぞれq∣P(J−1)、q∣P(J+1)に同値。
二つを引くとq∣2Pで、qは奇数だからq∣P。
逆は直ちに従う。□

一方だけの整数性では足りない。例えばq=25,b=1という局所的な計算で、
J≡6ならPの5進付値は1だがP(J−1)は25の倍数。
J≡24ならPの5進付値は0だがP(J+1)は25の倍数である。
これらは元の第一条件を満たす組として挙げたものではなく、
二つの多項式合同を一つだけに弱めてはいけない理由を示す局所例である。

(3)または(4)の既約分母に素数pが残れば、そのpはC(N,3)とC(N,J)の共通素因数になる。
実際、pはQ′₂の素因数であり、(5)の必要な素数冪で低位のKummer条件が破れるためである。
両方が整数になっても、Q′₁,Q′₂の**上位桁**の条件はなお別途必要である。

## 4. 引き継がれる素数でも上位桁で失敗する

既存の[緩和した三次族](i3_integrated_digits_and_center.md)のパラメータを20とすると

\[
n=3258244432,\quad j=1087547175,\quad
N=1629122216,\quad J=724542225.
\]

この組は8∣n、jとJは奇数、4≤j<n/2、4≤J<N/2、δ₁=δ₂=1、
元の二つの整除条件を満たす。
Q₂=(n−2)/2=5\cdot7\cdot53\cdot593\cdot1481。
p=53は旧Q₂と新Q′₁の双方に指数1で現れる。

53進桁を下位から並べると

| 整数 | 下位からの53進桁 |
|---|---|
| n | 2,20,25,49,41,7 |
| j | 1,13,0,44,31,2 |
| N | 1,10,39,24,47,3 |
| J | 1,0,38,43,38,1 |

jの全桁はnの対応する桁以下なので53∤C(n,j)。
一方、新しい組は第3桁で43>24となり、53∣C(N,J)。
**J≡1 (mod 53²)という強い低位の整除性があっても、上位桁は破れる。**

このnの2進付値は4であり、元の反例のu≥49やTの正規化を満たす例ではない。
反例全体に関する結論ではなく、二つの低位整除性と一素数での全桁条件からの
一般的な保存命題が偽であることを示す例である。

## 5. 同じ失敗を起こす無限族

さらに、k≥4として

\[
\begin{aligned}
n_k&=612+56108\cdot5^k,&j_k&=235+21580\cdot5^k,\\
N_k&=306+28054\cdot5^k,&J_k&=145+13280\cdot5^k
\end{aligned}
\tag{6}
\]

と置く。恒等式

\[
j_k(n_k-j_k)=(n_k-1)J_k
\]

より、n_k−1∣j_k(j_k−1)、N_k=n_k/2が成立する。
j_k,J_kは奇数で範囲内にある。

5進桁を下位から並べると

| 部分 | n側 | j側 |
|---|---|---|
| 定数項 | 2,2,4,4 | 0,2,4,1 |
| 5^kの係数 | 3,1,4,3,4,2,3 | 0,1,3,2,4,1,1 |

k≥4なら二つの桁群は重ならず、全ての桁でj_k≤n_k。
従って5∤C(n_k,j_k)。しかしN_kとJ_kの下位4桁はそれぞれ
1,1,2,2 と0,4,0,1なので、5∣C(N_k,J_k)。
またn_k−2≡10 (mod 25)、N_k−1≡5 (mod 25)だから、
5は旧C(n_k,3)と新C(N_k,3)の両方に指数1で現れる。

### n_kの2進付値を任意に大きくすることも可能

任意のU≥5を固定する。

\[
5^k\equiv-153\cdot14027^{-1}\pmod{2^{U-2}}
\tag{7}
\]

を満たせば、n_k=4(153+14027\cdot5^k)は2^Uの倍数。
右辺は1 (mod 4)なので、5が法2^(U−2)の1 mod 4の単元群を生成することから解kが存在する。
この事実はv₂(5^(2^r)−1)=r+2を帰納的に示しても証明できる。
解の周期は2^(U−4)。法8ではkは奇数であり、さらにk≡1 (mod 3)を課せる。
従ってk≡1 (mod 6)を満たす任意に大きい解kがある。

このときn_k≡1 (mod 9)なのでδ₁=δ₂=1。
ゆえに「十分高いv₂(n)を追加すれば、この素数での保存が成立する」という命題も偽である。

**ただし本来の正規化は満たさない。**
S=47+4316\cdot5^kとすればn_k=13S+1、j_k=5S、n_k≡2 (mod 5)なのでgcd(n_k,j_k)=1。
n_k≡1 (mod 9)とn_k≡2 (mod 5)は、n_kが2の冪であることとも両立しない。
したがって3∤n_kの奇数部分T>1はj_kを割らない。
この無限族は元の反例ではなく、第二の整除条件を全て満たすとも主張しない。

## 6. この結果が次の証明に要求するもの

式(1)は中心の整数を確かに更新するが、式(3),(4)には新しいn−4の因子が残る。
その整数性を証明しても、第4節のような上位桁の問題が別にある。
従って降下を続けるには、次の両方が必要である。

* n−4側で(3),(4)の両方の整数性を導くこと。
* 引き継がれる素数も含めて、上位桁のKummer条件を保存すること。

この証明にTの正規化、他の素数での全桁条件、またはnの冪の形を使う可能性は残る。
今回の例が否定するのは、それらを使わない一般の保存命題である。
Dの増大領域の排除や、i=3全体の有限化は今回も得られていない。

## 7. 再現

```
python -X utf8 audit_i3_descent_kummer_obstruction.py
```

[検算器](audit_i3_descent_kummer_obstruction.py)と
[結果](verification_i3_descent_kummer_obstruction.json)を保存した。
更新式、合同式の同値性、数値例と無限族の桁の証明を確認する。
高い2進付値の構成は法2^Uで検証し、巨大な5^k自体は生成しない。
