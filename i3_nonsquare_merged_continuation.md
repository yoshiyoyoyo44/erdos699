# Erdős 699：進捗の統合と非平方の枝の継続

2026-09-12。添付ファイルとローカルの成果を統合し、その先を計算・証明した記録。
**問題全体の完全証明ではない。** 以下の恒等式・整除補題・必要条件の証明と、
有限な証明書による排除を区別する。

**2026-09-13 追記：** 次の進展は `i3_boundary_and_fixed_blocks.md` に統合した。
本稿で残した M=1 の B=1 の例外を二つの四次方程式と Magma で排除し、
C=1 は新しい初等的不等式 δ₁δ₂C>4T で排除した。
従って omega(Q₂)≥3 は全ての M に拡張される。
また A,B,C のどれか一つを固定すると有限個の楕円曲線の整数点へ還元でき、
任意の固定 F に対して min(A,B,C)≤F の候補全体が有限になる。
本稿第5節末の例外に関する保留と第9節は、後続稿の最新状態を参照すること。

## 1. 統合した範囲と出典

問題は、任意の整数 `1 ≤ i < j ≤ n/2` について
\[
\gcd\left(\binom ni,\binom nj\right)
\]
が素数 `p ≥ i` を因数にもつか、である。
公開の [問題文](https://www.erdosproblems.com/699) と
[Formal Conjectures の定式化](https://github.com/google-deepmind/formal-conjectures/blob/main/FormalConjectures/ErdosProblems/699.lean)
を照合した。本稿は文献上の新規性を主張しない。

取り込み時のローカル HEAD と GitHub の HEAD は、いずれも
`ead953df46b75a30457f7fac61e2707691805eb1` だった。
添付ファイルはバイトを変えず `source_nonsquare_handoff_2026-09-12.md` に保存した。
添付内の「次に試すこと」は研究上の提案として読み、証明済みの事実とは区別した。

既存の成果は次の通りである。

* `discriminant_continuation.md`：判別式公式、全ての `i ≥ 205` の排除。
  証明は明記された二つの公刊素数評価と、厳密に再生できる有限証明書を使う。
* 同稿と以前の有限証明書：`i=3` の反例なら
  \[
  n=2^uM,\quad M\text{ は正の奇数},\quad u\ge43,\quad M^3<2^{u-2}.
  \tag{1}
  \]
* `i3_2adic_and_square_continuation.md`：偶数 `j=2^vJ` に対して
  \(2^{4v}M^3<2^{u+1}\)。奇数 `j` について
  \(w=\max(v_2(j-1),v_2(n-j-1))\) とすると \(2^wM^3<2^u\)。
* `i3_all_square_branches.md`：全ての奇数 `M` について
  \(j(n-j)/(n-1)\) が整数の平方になる枝を排除。
* 添付：対称な商、三つの剰余による分割、二番目の分割、四つの因子による
  表現、二つの平方整除関係。以下の第2–4節に記号を整理して統合する。

この継続では有限証明書も拡張・再検証し、(1) の `u≥43` を **u≥49** に更新した。

この継続の新しい内容は、第5節の**追加の整数商の証明と素因数分割の制約**、第6–7節の
**誤差因子を伴わない大きさの不等式と分岐別の改善**、第8節の有限証明書の拡張である。
`i=1,2` は既存の基本的議論で扱われるが、`3 ≤ i ≤ 204` の全体は残る。

## 2. 記号を重複させない正規化

`i=3` の反例を仮定する。次を置く。
\[
\gamma=\begin{cases}3&v_3(M)=1,\\1&v_3(M)\ne1,\end{cases}
\quad T=M/\gamma,\quad H=\gamma2^u,\quad n=HT,\quad j=kT.
\]
既存の Kummer 条件から `T | j`。`T` は奇数で、`4 ≤ j < n/2`。
添付は正規化した `n/T` と三つ目の剰余ブロックの両方に `C` を使っていたため、
ここでは前者を **H**、後者を **C** に統一する。

\[
\delta_1=\begin{cases}3&v_3(n-1)=1,\\1&\text{それ以外},\end{cases}
\quad
\delta_2=\begin{cases}3&v_3(n/2-1)=1,\\1&\text{それ以外},\end{cases}
\]
\[
Q_1=(HT-1)/\delta_1,\qquad Q_2=(HT/2-1)/\delta_2.
\]
可能な `(γ,δ₁,δ₂)` は `(1,1,1),(1,1,3),(1,3,1),(3,1,1)`。
`Q₁,Q₂,T` は互いに素、`Q₁,Q₂` は奇数である。必要条件は
\[
Q_1\mid k(H-k),\qquad Q_2\mid k(H/2-k)(H-k).
\tag{2}
\]
`Q₁` の各完全素数冪における `Tk` の剰余は `0,1`、
`Q₂` の各完全素数冪における剰余は `0,1,2` である。
これは必要条件であり、全ての上位桁での無繰り上がり条件と同値とは主張しない。

正の整数
\[
L=\frac{k(H-k)}{Q_1},\quad q=\delta_1k-TL,\quad
q'=\delta_1(H-k)-TL
\]
に対して
\[
qQ_1=k(Tk-1),\quad q'Q_1=(H-k)(T(H-k)-1),
\]
\[
qq'=L(T^2L-\delta_1),\qquad q+q'+2TL=\delta_1H.
\tag{3}
\]
特に `q,q′` の正値性も最初の二式から従う。

## 3. 二つの分割と四つの因子

`Q₂=ABC` とし、完全素数冪を `Tk ≡ 1,0,2` の順で `A,B,C` に割り当てる。
三者は互いに素。各ブロックで (3) を読むと
\[
AB\mid q,\qquad AC\mid q',\qquad BC\mid L,\qquad
A^2\mid T^2L-\delta_1.
\tag{4}
\]
最後の式は、`A` 上では `L` が単元で、`q,q′` の双方が `A` の完全素数冪を
含むことから得る。`δ₁` も `Q₂` 上で単元である。

次に `Q₁=RS` とし、`Tk ≡ 1` の完全素数冪を `R`、`Tk ≡ 0` のものを `S` に割り当てる。
互いに素なブロックをまとめることで、正の整数 `a,b,g,h` が存在して
\[
k=SBg,\qquad Tk-1=RAa,
\]
\[
H-k=RCh,\qquad T(H-k)-1=SAb.
\tag{5}
\]
従って
\[
L=BCgh,\quad q=ABag,\quad q'=ACbh,
\]
\[
\boxed{T^2BCgh-A^2ab=\delta_1,}
\tag{6}
\]
\[
\delta_1S=Aa+TCh,\qquad \delta_1R=Ab+TBg.
\tag{7}
\]
また (3)、(6)、`HT=2δ₂ABC+2` から
\[
\boxed{T(Bag+Cbh)+2Aab=2\delta_1\delta_2BC.}
\tag{8}
\]
添付の `a₀,b₀,c₀,d₀` は、それぞれ `ag,bh,gh,ab` である。
一般に積の等式だけからこの分解を仮定するのではなく、(5) が分解の根拠になる。

## 4. 主要分岐の訂正された表現と既存の下界

`T=δ₁=δ₂=1` とする。`Q=Q₂=ABC`、`H=2Q+2`。
`H` は16の倍数なので、(3) と `L ≡ k² (mod 2)` から `q,q′` は偶数。
`q=2r,q′=2r′` とすると
\[
4rr'=L(L-1),\qquad L+r+r'=Q+1.
\tag{9}
\]
各完全素数冪を法として、`L,r,r′` の一つだけが1、他は0になる。
正しい対応は
\[
L=BCx,\qquad r=ABz,\qquad r'=ACy,\qquad L-1=A^2w_0,
\]
\[
BCx-A^2w_0=1,\qquad Bz+Cy+Aw_0=BC,\qquad xw_0=4yz.
\tag{10}
\]
すなわち `x=gh,z=ag/2,y=bh/2,w₀=ab`。
これで添付が指摘した以前の `r,r′` の割り当ての誤りも訂正される。

`0<L,r,r′<Q` なので、どのブロックも1にはなれない。
例えば `A=1` なら `Q|L`、`B=1` なら `Q|r′` となる。従って
\[
A,B,C>1,\qquad \omega(Q)\ge3.
\]
`r=1` も (9) と剰余条件に反するため、`Q|r(r-1)` より
`r>√Q`。さらに
\[
r=\frac{k(k-1)}{2(2Q+1)},\qquad L>k/2
\]
から
\[
k>2Q^{3/4},\qquad L>Q^{3/4}.
\]
整数性から `L≤H/4=(Q+1)/2`。等号なら `(H-2k)²=H` なので `L` も平方になり、
既存の平方排除に反する。よって反例の主要分岐では
\[
Q^{3/4}<L<(Q+1)/2.
\]
また (6) は
\[
\det\begin{pmatrix}Aa&Ch\\Bg&Ab\end{pmatrix}=-1
\]
となる。二つの積 `Ch·Bg` と `Aa·Ab` は互いに素である。
この行列の減算が `H=2^u` または `3·2^u` の形を保存するとはまだいえない。

## 5. 新しい補題：追加の商は一般の T でも整数になる

添付では
\[
X=2Tq+T^2L,\quad Y=2Tq'+T^2L,
\quad B^2\mid X,\quad C^2\mid Y
\]
まで得ていたが、`g,h` を除いた商の整数性は未証明としていた。
**この整数性は、`gcd(B,g)=gcd(C,h)=1` を仮定せず証明できる。**

(5) を使うと
\[
R(2Aa+TCh)=2(Tk-1)+T(H-k)=n+j-2,
\]
\[
S(2Ab+TBg)=2(T(H-k)-1)+Tk=2n-j-2.
\tag{11}
\]
`B|n-2` かつ `B|j` だから最初の右辺は `B` の倍数。
`R|Q₁,B|Q₂` より **gcd(R,B)=1** なので
\[
B\mid 2Aa+TCh.
\]
同様に `C|n-2` かつ `C|n-j` と **gcd(S,C)=1** から
\[
C\mid 2Ab+TBg.
\]
従って正の整数
\[
\boxed{\eta=\frac{2Aa+TCh}{B},\qquad
\zeta=\frac{2Ab+TBg}{C}}
\tag{12}
\]
が定義できる。積を展開し (8) を使えば
\[
\boxed{\eta\zeta=T^2gh+4\delta_1\delta_2A.}
\tag{13}
\]
実際、左辺の分子と `BC(T²gh+4δ₁δ₂A)` の差は
\[
2A\{T(Bag+Cbh)+2Aab-2\delta_1\delta_2BC\}=0.
\]
添付の平方整除の商 `E=X/B²,F=Y/C²` も
\[
E=Tg\eta,\qquad F=Th\zeta,
\]
\[
EF=T^2gh(T^2gh+4\delta_1\delta_2A)
\]
と完全に整数で表せる。

さらに (6) と `gcd(A,δ₁)=1` から `gcd(A,T²gh)=1` なので
\[
\gcd(T^2gh,\eta\zeta)\mid4\delta_1\delta_2.
\tag{14}
\]
主要分岐では `gcd(gh,ηζ)` は、`k` が奇数なら1、偶数なら4に等しい。
後者では `g,h` が偶数で `gh` が4の倍数であることと (13) を使う。

さらに (12) を `B,C` の二元連立方程式として解くと
\[
\boxed{2\delta_1\delta_2B=a\zeta+Tbh,\qquad
2\delta_1\delta_2C=b\eta+Tag.}
\tag{14a}
\]
分母の行列式は (13) により `4δ₁δ₂A` で、`2A` が約分される。
ここで `η≡h,ζ≡g (mod 2)` であり、`a` と `g`、`b` と `h` はそれぞれ異なる偶奇を持つ。
従って右辺の各積 `aζ,bh,bη,ag` は正の偶数であり、
\[
\boxed{\delta_1\delta_2B\ge T+1,\qquad
\delta_1\delta_2C\ge T+1.}
\tag{14b}
\]
`T≥5` または `δ₁δ₂=1` なら `B,C>1` が従う。
第6節では全ての分岐で `A>1` も証明するので、反例において
\[
\boxed{M>1\ \Longrightarrow\ \omega(Q_2)\ge3.}
\tag{14c}
\]
実際、`M>1` なら `T≥5`、または `M=3,T=1,δ₁=δ₂=1` である。
`M=1,δ₁=δ₂=1` でも同じ結論が成り立つ。
`M=1,δ₁δ₂=3` の場合に `B,C>1` を一律に主張してはいない。

**添付の障害に対する検算。** 既存の緩和例
`n=76672,j=26775,T=δ₁=δ₂=1` では
\[
(A,B,C,R,S)=(11,85,41,1217,63),\quad(a,b,g,h)=(2,72,5,1).
\]
実際に `gcd(B,g)=5>1` だが、(12) は `η=1,ζ=49` を与え、
`ηζ=5+4·11=49`。したがって新証明は、添付で問題視された重複素因数を許容する。

ただし、これで**無限降下が成立したわけではない**。
この緩和例でも `η<g` と `ζ<h` は同時に成立しない。
新しい整数商を次の反例に組み直せること、特別な `H` の形と全ての Kummer 条件を
保存することは、いずれも別途証明が必要である。

## 6. 新しい大きさの補題

以下は `16|n`、`4≤j<n/2`、(2) と上の分割だけから従い、
`H=γ2^u` の形をまだ使わない。

まず **BC>δ₁** を示す。`BC=1` なら `A=Q₂` で `j≡1 (mod Q₂)`。
`δ₂=1` の場合、`j<n/2=Q₂+1` に反する。
`δ₂=3` なら `δ₁=1` であり、`j=ℓQ₂+1`、`ℓ=1,2` のみが残る。
ところが `Q₁=6Q₂+1|j(j-1)` を36倍すると
`Q₁|ℓ(ℓ-6)` となり、`Q₁>8` に反する。
よって `BC>1`。`δ₁=3` なら `3∤Q₂` なので、奇数 `BC>1` は5以上である。

`x=gh,p=ab` と置く。整数
\[
W=T^2L=\frac{\delta_1j(n-j)}{n-1}
\]
は
\[
W\le\frac{\delta_1n^2}{4(n-1)}<\frac{\delta_1n}{4}+1
\]
を満たす。`δ₁n/4` も整数だから `W≤δ₁n/4`。
`n=2δ₂ABC+2` を代入すると
\[
BC(2T^2x-\delta_1\delta_2A)\le\delta_1.
\]
括弧の中は奇数。`BC>δ₁` なので正ではあり得ず、従って
\[
\boxed{\delta_1\delta_2A>2T^2x.}
\tag{15}
\]
これから **A>1** も従う。`A=1` なら `δ₁δ₂≤3` より `T=x=1,δ₁δ₂=3`。
`g=h=1` なので `k` は奇数で、(17) により `ab≡0 (mod 8)` である。
一方、(6) は `ab=BC-δ₁` を与える。
`δ₂BC=n/2-1≡7 (mod 8)` だから `ab≡7δ₂-δ₁≡4 (mod 8)` となり、矛盾する。

(6) に `A` を掛けると
\[
T^2Q_2x=A^3p+\delta_1A>A^3p.
\]
(15) を代入して
\[
Q_2>\frac{8pT^4x^2}{\delta_1^3\delta_2^3},
\]
すなわち
\[
\boxed{(n-2)\delta_1^3\delta_2^2>16abT^4(gh)^2.}
\tag{16}
\]
旧稿の `T²-δ₁` による不等式と異なり、`T=1` でも退化しない。
この補題自体は第5節の `η,ζ` を必要とせず、統合された四因子表現から得られる。

## 7. 反例の奇数部分と2進指数の改善

`H=γ2^u,n=2^uM,T=M/γ` を戻す。`v=v₂(j)=v₂(k)`。
`R,S,B,C` が奇数なので
\[
v_2(g)=v_2(h)=v,\qquad gh\ge2^{2v}.
\]
(6) を mod 8 で読むと
\[
ab\equiv\delta_1(k^2-1)\pmod8.
\tag{17}
\]
`v=1` なら `ab≥3` (`δ₁=1`) または `ab≥1` (`δ₁=3`)。
`v≥2` ならそれぞれ `ab≥7` または `ab≥5`。
従って (16) から、偶数 `j` について
\[
\boxed{2^{4v}M^3<c\,2^u}
\tag{18}
\]
が得られ、定数は次の通り。

| γ | δ₁ | δ₂ | v=1 の c | v≥2 の c |
|---|---|---|---|---|
| 1 | 1 | 1 | 1/48 | 1/112 |
| 1 | 1 | 3 | 3/16 | 9/112 |
| 1 | 3 | 1 | 27/16 | 27/80 |
| 3 | 1 | 1 | 27/16 | 81/112 |

全行で従来の `c=2` より小さい。例えば `M=1,δ₁=δ₂=1,v≥2` なら
\[
\boxed{u\ge4v+7,}
\]
従来の `v≤floor(u/4)` を改善する。全分岐でも `v≥2` なら
`c≤81/112<1`、さらに `γ=3` の行では `M≥3` を使い、**u≥4v+2** が得られる。

`j` が奇数のときは、`j-1` と `n-j-1` の和が `n-2≡2 (mod 16)` なので
一方の2進指数は1、他方は `w≥2`。式 (5) の奇数の分母から
\[
v_2(ab)=w+1.
\]
よって
\[
\boxed{2^wM^3<\frac{\gamma^4\delta_1^3\delta_2^2}{32}\,2^u.}
\tag{19}
\]
`γ=1` の三行の係数は `1/32,9/32,27/32` で、従来の1を改善する。
`γ=3` では従来の係数1を引き続き使う。

`j` の偶奇を問わず `ab(gh)²≥8` だから
\[
M^3<\frac{\gamma^4\delta_1^3\delta_2^2}{128}\,2^u.
\]
既存の (1) と併用すると

| (γ,δ₁,δ₂) | 新しい一様な必要条件 |
|---|---|
| (1,1,1) | M³ < 2ᵘ/128 |
| (1,1,3) | M³ < 9·2ᵘ/128 |
| (1,3,1) | M³ < 27·2ᵘ/128 |
| (3,1,1) | M³ < 2ᵘ/4（既存の評価を採用） |

この改善も `M=1,u→∞` を排除するものではない。

## 8. 有限証明書の拡張と検証方法

`extend_i3_certificate.py` は既存の必要条件 `M³<2^(u-2)` だけを使い、
`43≤u≤48` の全ての奇数 `M` と、第1合同条件の全ての CRT 根を列挙する。
この有限排除は、第5–7節の新補題には依存しない。

生成結果は `i3_u43_to_u48.json`、再帰的な Lucas 素数証明書は
`prime_certificates_u48.json` に保存する。標準 Python だけで再検証するコマンドは

```text
python replay_certificate.py i3_u43_to_u48.json --primes prime_certificates_u48.json --output verification_u48.json
```

検証側は候補を素数冪の二分割から独立に再構築し、全奇数 `M` の被覆を確認する。
素数性も保存された判定の真偽値を信じず、`p-1` の完全因数分解、
再帰的に証明された素数、剰余冪と gcd による位数条件から証明する。
新しいファイルの最終検証結果は `verification_u48.json` を参照する。

再検証済みの結果は次の通り。

| u | 検査した奇数 M の数 | 第1条件の候補数 | 第2条件を通過 |
|---|---|---|---|
| 43 | 6,502 | 34 | 0 |
| 44 | 8,192 | 121 | 0 |
| 45 | 10,321 | 63 | 0 |
| 46 | 13,004 | 53 | 0 |
| 47 | 16,384 | 33 | 0 |
| 48 | 20,643 | 291 | 0 |
| 合計 | 75,046 | 595 | 0 |

177,312個の素数を再帰的に証明した。既存の `u≤42` の証明書も、検証プログラムの
拡張後に再実行して以前と一致した。従って最新の一様な必要条件は
\[
\boxed{n=2^uM,\qquad u\ge49,\qquad M\text{ は正の奇数},\qquad M^3<2^{u-2}.}
\]
第7節の分岐別条件は、これに追加して適用できる。
大きい添字の検証 `replay_large_indices.py` も再実行し、全38,025区間と最後の12組の
排除が通過した。

代数と整除性の補助検算は

```text
python audit_i3_nonsquare_lifts.py
```

で再生する。10個の記号的恒等式を検査し、`16≤n≤1,000,000,16|n` で第1条件の
173,844候補を独立に列挙する。この緩和範囲で第2条件まで残るのは上記の
`(76672,26775)` の1例であり、重複素因数を含む整除補題を実際に検算できる。
**この有限検算には、他の δ 分岐や T>1 の正例は含まれていない。**
それらを含む一般の証明は第2–7節の議論であり、有限な正例からの一般化ではない。
新しい Lean 形式化は行っていない。

## 9. 完全解決に向けて残る論点

追加の整数商 (12)–(13) は証明できた。しかし、これを反復できる降下へ変える
写像は得られていない。反復には少なくとも正値性・整数性だけでなく
`H=γ2^u` と全ての桁の Kummer 条件の保存が必要である。

次の焦点は、(6)、(8)、(12)、(15) と
\[
\delta_2ABC+1=\gamma T2^{u-1}
\]
を同時に使い、特別な形を保つ変換または `u` の上限を得ることである。
`T=1,δ₂=1` なら、添付で着目された `ABC+1=2^m` または `3·2^m` に戻る。
一般の場合に `T` や `δ₂` を落としてはならない。

現段階では、非平方の全枝、`i=3` 全体、さらに `4≤i≤204` は未解決である。
新しい商の整数性や有限計算の上限の拡張を、問題全体の解決とは呼ばない。
