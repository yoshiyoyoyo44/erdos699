# Erdős 699：桁和6の排除、中心への整除性の訂正、桁和12の一般下界

2026-09-19。GitHub とローカルの基準は
`6ce37996abdf643acb4a8d9bb7713176b3d2c9fc`。
**i=3 全体および問題699全体の完全解決には到達していない。**
本稿の新しい結論は、明示した仮定の下で全指数に対して成立する必要条件である。
文献上の優先性や Lean による形式検証は主張しない。

続く [全六ブロックと桁和の移行の稿](i3_global_digit_constraints_2026-09-19.md)では、
T=1 を含む総桁和の下界、合同類の排除、γ=3・3∤j の奇数 j への拡張を証明した。

## 1. 資料と今回の結論

添付メモを [原資料](source_i3_progress_summary_2026-09-18.md) にバイトを変えず保存した。
SHA-256 は `48a1d23abb83cdc5be0df9920ee59dcb35b5446611f06fbe280987acad2d5875`。
文中の研究方針を前提とはせず、以下の各命題を証明し直した。

今回の主な結果は次の通り。

* **v₂(j)≥3 なら、T のどの素因数でも対応する商の桁和6は不可能。**
* v₂(j)≥2 の桁和4から **δ₁=3、γ=1、p≡7 (mod 12)** が従う。
  添付の素数冪 T に対する議論を、任意の T に広げられる。
* 桁和8が (4,4) に分かれるとき、中心への整除性を p≡3 (mod 8) にも拡張できる。
  一方、添付の p≡5 (mod 8) の証明には **p=5、γ=3、3∤j** の例外が必要。
* **γ=3、3∤j、v₂(j)≥3 なら、p≠5 で桁和≥12、p=5 で桁和≥8。**
  T に含まれる素因数の個数を制限しない。
* 素数冪 T=5^e の桁和4では、v₂(u)≤v₂(e) に改善でき、
  **u の奇数部分が7以下の場合を全て排除**できる。

一般の問題文は
[Formal Conjectures の定式化](https://github.com/google-deepmind/formal-conjectures/blob/main/FormalConjectures/ErdosProblems/699.lean)
とも照合した。そこでの条件は p≥i である。

## 2. 使用する正規化

[既存の全桁・相互法則の稿](i3_digit_reciprocity_and_W_continuation.md)の記号を使う。
i=3 の反例があると仮定すると

\[
n=\gamma T2^u=TH,\qquad H=\gamma2^u,\qquad j=Tk,
\]
\[
Q_1=(n-1)/\delta_1=RS,\qquad Q_2=(n/2-1)/\delta_2=ABC.
\]

(γ,δ₁,δ₂) は (1,1,1),(1,1,3),(1,3,1),(3,1,1)。
六ブロックは互いに素な奇数で、gcd(T,γδ₁δ₂)=1。
特に γ=3 なら 3∤T。既存の必要条件には

\[
Q_1\mid j(n-j),\quad
k=SBg,\quad H-k=RCh,
\quad M=\gamma T,\quad M^3<2^{u-2},
\]

u≥49、および偶数 j で u≥4v₂(j)+14 が含まれる。
また基準稿の A,B,C≥11、R,S>1 により、各加数は p と互いに素な非自明な奇数因子を持つ。
従って 2p^a 型の加数は v=1 の場合も不可能である。
既存の巨大な有限証明書や Magma の整数点計算全体を今回再実行したわけではない。

以下、j は偶数、v=v₂(j)=v₂(k) とする。
p^e∥T、U=T/p^e に対し

\[
X=Uk,\qquad Y=U(H-k),\qquad N=X+Y=U\gamma2^u.
\tag{1}
\]

Kummer の条件より、この加法は p 進で繰り上がりを持たない。したがって

\[
d_p:=s_p(N)=s_p(X)+s_p(Y).
\tag{2}
\]

u>v なので v₂(X)=v₂(Y)=v。さらに

\[
\varepsilon=\gcd(k/2^v,\gamma)\in\{1,3\},\qquad
\gcd(X,Y)=U2^v\varepsilon.
\tag{3}
\]

γ=3 では ε=1 と 3∤j が同値である。
また p∤N であり、gcd(U,Q₁)=1。

## 3. 桁和2と4は同じ高い2進付値を持てない

### 補題1：一つの加数の桁和

p は奇素数、Z>0、v₂(Z)=v≥3 とする。

1. s_p(Z)=2 なら、p≡3 (mod 4)、v₂(p+1)=v。
2. s_p(Z)=4、p≡3 (mod 4) なら

   \[
   p+1\mid Z,\qquad
   Z\equiv2(p+1)\pmod{p^2-1},\qquad
   v_2(Z)>v_2(p+1).
   \tag{4}
   \]

**証明。** 桁和2の数は 2p^a または p^a(1+p^r), r≥1。
前者の2進付値は1なので除外される。r が偶数なら p^r+1≡2 (mod 8)。
従って r は奇数で、v₂(p^r+1)=v₂(p+1)=v となる。

桁和4の場合、偶数位置の桁の和を E、奇数位置の桁の和を O とする。
E+O=4、p²≡1 (mod 8) より

\[
0\equiv Z\equiv E+pO=4+(p-1)O\pmod8.
\]

p≡3 または7 (mod 8) のどちらでも O=2、E=2 に限られる。
従って p²−1 を法として Z≡2+2p となり、最初の二式を得る。
w=v₂(p+1)≥2 とすれば v₂(p²−1)=w+1 であり、
2(p+1) も 2^{w+1} の倍数なので v≥w+1。□

### 定理1：桁和6の完全排除

\[
\boxed{v_2(j)\ge3\quad\Longrightarrow\quad d_p\ne6
\quad\text{for every }p\mid T.}
\tag{5}
\]

**証明。** p は奇数、X,Y は正の偶数なので各桁和は正の偶数である。
合計6なら分割は (2,4) または (4,2)。桁和2の側は補題1より
v₂(p+1)=v を要求するが、桁和4の側は同じ補題より v₂(p+1)<v を要求する。矛盾。□

この証明は上限付きの指数探索を使わず、T が素数冪であることも使わない。
v=1,2 について桁和6を全排除したという意味ではない。

### 一般の2進下界の確認

任意の Z>0 について Z≡s_p(Z) (mod p−1)。従って
2^{min(v₂(Z),v₂(p−1))} は正の整数 s_p(Z) を割る。
二つの加数に適用して、添付の下界

\[
\boxed{d_p\ge2^{1+\min(v,v_2(p-1))}}
\tag{6}
\]

を得る。これも全指数の証明である。

## 4. 桁和4の相互法則を一般の T に拡張する

v≥2、d_p=4 を仮定する。各加数の桁和は2であり

\[
X=p^a(1+p^r),\qquad Y=p^b(1+p^s)
\]

と書ける。2p^a 型は v≥2 に反し、r,s は奇数、p≡3 (mod 4)。

任意の奇素数 ℓ|Q₁ は X,Y の少なくとも一方を割る。
実際、Q₁|j(n−j)、gcd(Q₁,TU)=1 を使えばよい。
したがって p のある奇数冪が −1 (mod ℓ) となり

\[
\left(\frac p\ell\right)=\left(\frac{-1}\ell\right).
\]

平方剰余の相互法則と p≡3 (mod 4) から (ℓ/p)=1。
素因数の重複も含めて掛ければ (Q₁/p)=1 となる。一方、
Q₁≡−δ₁^{-1} (mod p) だから (−δ₁/p)=1。
δ₁=1 は (−1/p)=−1 に反する。結局

\[
\boxed{v\ge2,\ d_p=4\quad\Longrightarrow\quad
\delta_1=3,\ \gamma=1,\ p\equiv7\pmod{12}.}
\tag{7}
\]

最後の合同式は p≠3、p≡3 (mod 4)、(3/p)=−1 から従う。
特に γ=3 または 3|T では、v≥2 の桁和4は全て不可能。
**一般の T で v=2 自体を排除したわけではない。**

同じ議論を ℓ|U に適用すると (ℓ/p)=1。
これはもう一方の加数の桁和を制限せず、
「どちらか一方の加数の桁和が2」という仮定だけで成り立つ。
従って v≥2 では、そのような素数 p は T の中に高々一つ。
二つあれば両者が3 mod 4で、相互法則に反して互いに平方剰余になってしまう。

なお d_p=4 では、d=gcd(r,s) を用いると既存の gcd 公式から
U2^v=p^d+1 を得る（γ=1 なので ε=1）。p≡1 (mod 3) と
n≡1 (mod 3) を合わせると u+v は奇数であることも従う。

## 5. 桁和8から中心への整除性：例外を明記した形

v≥3、s_p(X)=s_p(Y)=4 を仮定する。
(6) により p≡1 (mod 8) は不可能で、残るのは p≡3,5,7 (mod 8)。

### 5.1 p≡3 (mod 4)

補題1より

\[
X\equiv Y\equiv2(p+1)\pmod{p^2-1},\qquad v>v_2(p+1).
\tag{8}
\]

これは添付の p≡7 (mod 8) に加えて **p≡3 (mod 8)** も扱う。
また添付の v≥v₂(p+1) は、ここでは厳密に v>v₂(p+1) に強まる。

w=v₂(p+1)、m=(p+1)/2^w とすると (3),(8) から m|Uε。
p が T の最小素因数なら m<p なので m の素因数は U を割れず、m|ε。
よって p=2^w−1 または p=3·2^w−1、かつ v≥w+1。
後者には ε=3 が必要である。

### 5.2 p≡5 (mod 8)

各加数の E,O は E+O=4 と 8|Z より、(E,O)=(3,1) または (1,3)。
したがって、各加数は p²−1 を法として

\[
p+3\quad\text{または}\quad3p+1
\]

に等しい。同じ型なら X≡Y (mod p²−1) となる。

異なる型なら X+Y≡4(p+1) (mod p²−1) なので p+1|Uγ2^u。
一方、X≡±2 (mod p+1) と U|X より gcd(U,p+1)=1。
m=(p+1)/2 は奇数で、m|γ。しかも m≡3 (mod 4) だから

\[
\boxed{p=5,\qquad\gamma=3.}
\]

この場合 X≡±2 (mod 3) なので ε=1。
従って正しい結論は

\[
\boxed{X\equiv Y\pmod{p^2-1}
\quad\text{または}\quad(p,\gamma,\varepsilon)=(5,3,1).}
\tag{9}
\]

### 5.3 添付の局所推論に対する具体例

\[
p=5,\quad U=1,\quad\gamma=3,\quad u=9,
\]
\[
X=760=5^4+5^3+2\cdot5,\qquad
Y=776=5^4+5^3+5^2+1.
\]

両方の桁和は4、v₂(X)=v₂(Y)=3、gcd(X,Y)=8。
繰り上がりなしに X+Y=3·2^9 となるが、X−Y=−16 は24の倍数ではない。
e=1 とすれば n=7680、j=3800、c=n/2−j=40 なので c/5=8 は12で割れない。

**これは問題699の反例ではない。** u≥49、M³<2^{u−2}、Q₁ の整除性などを満たさない。
示したのは、添付第12節の桁の議論だけから例外なしの整除性は出せない、という点である。
例外が元の問題の全条件で消える可能性は残るが、その追加証明は得ていない。

### 5.4 中心の条件としての正しい使い方

c=n/2−j、z=c/T=H/2−k とすると X−Y=−2c/p^e。
(9) の例外を除けば

\[
\boxed{\frac{p^2-1}{2}\mid\frac c{p^e}=Uz.}
\tag{10}
\]

該当する各素数について

\[
D_p=\frac{(p^2-1)/2}{\gcd((p^2-1)/2,U)}
\]

と置けば、全てをまとめて lcm(D_p)|z とできる。
分母から U を除く必要があり、単純に各 (p²−1)/2 の積が z を割るわけではない。

## 6. γ=3、3∤j における桁和12の一般下界

### 定理2

γ=3、ε=1、v≥3 とする。このとき、任意の p|T について

\[
\boxed{d_p\ge
\begin{cases}
8,&p=5,\\
12,&p\ne5.
\end{cases}}
\tag{11}
\]

**証明。** 3∤U かつ ε=1 なので 3∤X,Y。また p≠3。

まず p≡1 (mod 3) なら N≡s_p(N) (mod 3) より 3|d_p。
d_p は偶数で4以上。従って12未満の可能性は6だけであり、定理1に反する。

次に p≡2 (mod 3)、p≡3 (mod 4) とする。
加数の桁和が2なら、補題1の奇数 r により p+1 がその加数を割る。
桁和4でも補題1により p+1 がその加数を割る。
3|p+1 なので両方とも 3∤X,Y に反する。各桁和は従って6以上で、合計12以上。

残る p≡2 (mod 3)、p≡1 (mod 4) では各桁和は4の倍数。
12未満なら両方4に限る。p≡1 (mod 8) は (6) に反するので p≡5 (mod 8)。
第5節の二つの型が同じなら X+Y≡±4 (mod p+1)。
しかし 3|p+1、3|X+Y なので不可能。異なる型なら第5節の例外に一致して p=5。
これで全ての素数を尽くした。□

従って、この分岐では桁和8となる素数は5だけであり、桁和10も存在しない。
添付の「小さい桁和を持つ素数は高々一つ」という個数制約より強い、各素数への下界である。

ω(T) を異なる素因数の個数、[5|T] を指示関数とすれば

\[
\boxed{\sum_{p\mid T}d_p\ge12\omega(T)-4[5\mid T].}
\tag{12}
\]

さらに (6) も併用すると

\[
\sum_{p\mid T}d_p\ge12\omega(T)-4[5\mid T]
+\sum_{\substack{p\mid T\\v_2(p-1)\ge3}}
\left(2^{1+\min(v,v_2(p-1))}-12\right).
\tag{13}
\]

γ=1 または 3|j の分岐に、この12という下界をそのまま適用することはできない。

## 7. 素数冪 p=5 の桁和4に対する追加の全指数排除

この節は N=2^u、T=p^e、γ=1、d_p=4 の場合を扱う。
二つの加数は p^a(1+p^r), p^b(1+p^s) の形なので、
全ての ℓ|Q₁ について ord_ℓ(p) は偶数である。
ここでは r,s の偶奇や最下位桁が1であることを仮定しない。

### 補題2：p≡2 (mod 3) では v₂(u)≤v₂(e)

t=v₂(u)、h=v₂(e) とする。t≥h+1 と仮定して

\[
a=u/2^h,\qquad b=e/2^h,\qquad D=2^a p^b-1
\]

と置く。a は正の偶数、b は奇数であり、D|n−1。
p≡2 (mod 3) より D≡1 (mod 3)、また D≡3 (mod 4)。
従って D|Q₁ で、D の素因数には ℓ≡3 (mod 4) が存在する。

その ℓ で p^b≡2^{-a} は平方剰余。b が奇数なので p も平方剰余である。
従って ord_ℓ(p)|(ℓ−1)/2 は奇数。前述の必要条件と矛盾する。よって

\[
\boxed{p\equiv2\pmod3\quad\Longrightarrow\quad v_2(u)\le v_2(e).}
\tag{14}
\]

この補題は、原資料の位数障害の v₂(e)+1 を p≡2 (mod 3) で改善する。
補題自体は p の大きさや BBM の数値定数を使わない。

### p=5、u の奇数部分が7以下の場合

u=2^t U₀、U₀ は正の奇数とする。(14) より e=2^t b、b≥1。
既存の M³<2^{u−2} から

\[
5^{3b}<2^{U_0}.
\tag{15}
\]

従って U₀≤5 は不可能。U₀=7 なら 5³<2⁷<5⁶ より b=1。
すると

\[
n=(2^7\cdot5)^{2^t}=640^{2^t}.
\]

71 は素数で、71|640−1、5⁵≡1 (mod 71)、5≢1 (mod 71)。
よって 71|Q₁ で ord₇₁(5)=5 は奇数となり矛盾する。

\[
\boxed{T=5^e,\ \gamma=1,\ d_5=4\quad\Longrightarrow\quad
U_0\ge9.}
\tag{16}
\]

これは全ての t≥0 を扱う。U₀≥9 の p=5 分岐を閉じたという意味ではない。

## 8. 再現用検算

標準 Python だけで次を実行できる。

```text
python -X utf8 audit_i3_low_digit_continuation.py
```

結果は [verification_i3_low_digit_continuation.json](verification_i3_low_digit_continuation.json)。
assert を無効にする `-O` は使わない。

* 80万個の (p,Z) で桁和・交代桁和・付値の補題を検査。
* 3,984組の素数対を検査し、奇数冪が−1となる1,335組で相互法則の帰結を確認。
* 108,808個の (p,γ,U,u) を走査。桁和が4,6,8,10,12の和について、
  条件に合う全ての桁の分割を列挙し、206,001分割を検査。
* 等しい2進付値を持つ48,110分割で、桁和6がないことを確認。
* U による整除性まで満たす1,927分割で中心の分類などを検査。
  (4,4) の131分割は通常の整除性94件と明記した例外37件に分かれる。
  γ=3、ε=1 の436分割では、定理2の下界を確認した。
* 補題2は小さいパラメータの192ケースで、試し割りと剰余冪だけで奇数位数の素因数を確認。
  71による U₀=7 の排除の算術も再生する。

これらは一般証明を補助する有限検算である。
列挙された局所モデルは Q₁、Q₂ や全ブロックの条件を満たす反例候補の一覧ではない。
検算範囲を超える一般命題の根拠は第3–7節の証明である。

## 9. 未解決の部分

得られた桁和の下界に対応する一様な上界は、まだ得られていない。
第5節の中心の整除性も、既存の中心の下界と併存し得る。
それらを並べるだけでは矛盾にならず、T=1 には T の素因数の条件そのものがない。

従って一般の非平方の分岐、奇数 j、T=1、素数冪の残存分岐は未解決。
添付で保留されている BBM の定数・有限探索の完全性を、本稿の証明に使っていない。
既存の u≥49 や解決済み添字の範囲は今回一律には更新しない。

今回保存したのは、上の証明、局所推論の例外とその修正、再現可能な検算である。
これを問題699の完全解決と呼ぶ根拠はない。
